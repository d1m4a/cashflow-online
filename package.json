{
  "name": "meshok-denyak-online",
  "version": "0.1.0",
  "private": true,
  "description": "Online board game prototype about capital, reputation, and project goals.",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node backend/server.js",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.21.0"
  }
}
